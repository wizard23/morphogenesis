# Performance progress notes

One standalone note per measurement: `<yyyy>/<mm>/<ISO-timestamp>--<tag>.md`. Include: what
changed, why, exact commands, the harness header block (machine / profile / zig / git / wasm sha),
pasted result tables, interpretation, keep / revert / inconclusive. See `docs/HOWTO-performance.md`.
